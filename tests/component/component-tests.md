# UI Tests

- This test md file outlines the UI tests for this website.
- There are 4 major UI sections of this site: header, main, FAB, footer
- Each section below (except main, which has its own folder `main/` and md file) has its own test file: `<section>-tests.spec.ts`
